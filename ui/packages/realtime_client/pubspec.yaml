name: realtime_client
description: Supabase Realtime channel client with polling fallback for M&S Delivery System
version: 1.0.0
publish_to: none

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  api_client:
    path: ../api_client
  domain_models:
    path: ../domain_models

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
